import React from "react";
import Title from "../components/Title/Title";

const NotFoundPage = () => (
  <div className="NotFoundPage">
    <Title align="center">Sorry, the requested page could not be found.</Title>
  </div>
);

export default NotFoundPage;
